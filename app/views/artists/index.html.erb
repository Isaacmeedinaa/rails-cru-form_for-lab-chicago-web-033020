<style>
    h1, p, a {
        font-family: Arial, Helvetica, sans-serif;
    }

    p {
        font-size: 18px;
    }
</style>

<h1>All Artists:</h1>

<strong>
    <p style="font-size: 22px;"><%= link_to 'Create a new artist', new_artist_path %></p>
</strong>

<% @artists.each do |artist| %>
    <p>
        <%= link_to("#{artist.name}", artist_path(artist)) %>
    </p>
<% end %>